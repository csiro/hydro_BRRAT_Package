% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/table_helpers.R
\name{export_BRRAT_csv}
\alias{export_BRRAT_csv}
\title{Export global and per-site summaries to CSV}
\usage{
export_BRRAT_csv(
  fit_obj,
  file_global = "lobal.csv",
  file_per_id = "per_id.csv",
  level = 0.95
)
}
\arguments{
\item{fit_obj}{A fitted object returned by \code{\link[=BRRAT]{BRRAT()}}, of class \code{BRRAT}.}

\item{file_global}{Path/filename for the global table CSV (default \code{'global.csv'}).}

\item{file_per_id}{If applicable to the model, Path/filename
for the per-site table CSV (default \code{'per_id.csv'}).}

\item{level}{Credible interval level passed to \code{\link[=summarise_BRRAT]{summarise_BRRAT()}} (default \code{0.95}).}
}
\value{
(Invisibly) a list with the two data frames written:
\verb{list(global = <data.frame>, per_id = <data.frame>)}.
}
\description{
Writes the output of \code{\link[=summarise_BRRAT]{summarise_BRRAT()}} to two CSV files: one for the
population slope (\code{file_global}) and one for per-site coefficients (\code{file_per_id}).
}
\details{
Export summary tables to CSV files
}
\examples{
\dontrun{
res <- BRRAT(Sim,Obs)
export_BRRAT_csv(res,
  file_global = "qerr_global.csv",
  file_per_id = "qerr_per_id.csv",
  level = 0.95
)
}

}
